<p id="notice"><%= notice %></p>
<div class="the__content">
  <h1>Ипотечные продукты и опции</h1>

  <h2>Действующие</h2>
    <% @products.each do |product| %>
      <p><strong><%= link_to product.name, edit_product_path(product) %></strong></p>
      <ul><li><%= product.note %>
      </li></ul>
    <% end %>
</div>
<br>

<%= link_to 'New Product', new_product_path %>
