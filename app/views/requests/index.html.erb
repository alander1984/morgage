<h1>Список Заявок</h1>

<table class="table">
  <thead>
    <tr>

    </tr>
  </thead>

  <tbody>
    <% @requests.each do |request| %>
      <tr>
        <td><td><%= link_to request.to_s, request, method: :get %></td></td>
      </tr>
    <% end %>
  </tbody>
</table>

